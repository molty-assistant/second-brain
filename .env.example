# Copy this file to .env.local and fill in your values.

# Optional Basic Auth for protecting the app in non-local environments.
# BASIC_AUTH_USER=your-username
# BASIC_AUTH_PASS=your-password

# Convex deployment URL used by the client and server when no server override is set.
# NEXT_PUBLIC_CONVEX_URL=https://<your-deployment>.convex.cloud

# Optional server-side Convex URL override (used by scripts/server utilities).
# CONVEX_URL=https://<your-deployment>.convex.cloud
