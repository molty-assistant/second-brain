import { api } from '../../convex/_generated/api';

// Use Convex codegen output for strongly-typed query/mutation references.
// This requires `npx convex codegen` to have been run (tracked in repo).
export const convexApi = api;
